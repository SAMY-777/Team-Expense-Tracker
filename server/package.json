{
  "name": "team-expense-tracker-server",
  "version": "1.0.0",
  "description": "Express + PostgreSQL backend for the Team Expense Tracker app",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "db:schema": "node db/runSql.js schema.sql",
    "db:seed": "node db/runSql.js seed.sql",
    "db:setup": "npm run db:schema && npm run db:seed",
    "test": "echo \"No automated tests configured - see README for manual test steps\" && exit 0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
